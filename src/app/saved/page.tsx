import SavedClient from "./SavedClient";

export const metadata = {
  title: "Saved Looks | Private Client Wardrobe | TSquare Clothing Cafe",
  description:
    "Review your curated lookbook of saved TSquare silhouettes and initiate a consultation with our tailoring house.",
};

export default function SavedPage() {
  return <SavedClient />;
}
