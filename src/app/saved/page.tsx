import SavedClient from "./SavedClient";

export const metadata = {
  title: "Saved Looks | Private Client Wardrobe | TSquare Clothing Cafe",
  description:
    "Review your curated lookbook of saved TSquare silhouettes and initiate consultation with our Abeokuta tailoring house.",
};

export default function SavedPage() {
  return <SavedClient />;
}
